To run gateway as service put file "gateway.service" in /etc/systemd/system/.

To start a service type in terminal console:
$ systemctl start service.gateway

To enable a service to start on boot type in terminal console:
$ systemctl enable service.gateway